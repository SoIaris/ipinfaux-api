# ipinfaux api
this just converts the package ipinfaux into a api that anybody can use

## running
```bash
pip install -r requirements.txt

flask run --port 5000
```
i recommend just using vercel to host this simple api or you can use the already existing one on vercel

## packages
[ipinfaux](https://pypi.org/project/ipinfaux/)